# วิธีเชื่อม Excel → Google Sheets → Dashboard (ฟรี)

เว็บไซต์นี้อ่านยอดขายจาก Google Sheets ที่เผยแพร่เป็น CSV โดยไม่ต้องเสียค่าบริการและไม่ต้องทำเซิร์ฟเวอร์เพิ่ม

## 1. เตรียมไฟล์ Excel

1. เปิด Google Drive แล้วเลือก **ใหม่ → อัปโหลดไฟล์** เพื่ออัปโหลด Excel เดิม
2. คลิกขวาไฟล์ → **เปิดด้วย → Google Sheets**
3. ตั้งชื่อแท็บข้อมูลว่า `Sales` และจัดคอลัมน์ให้ตรงตามนี้ (ตัวพิมพ์เล็ก/ใหญ่ไม่สำคัญ):

| date | product | litres |
|---|---|---:|
| 2026-09-01 | g95 | 1200 |
| 2026-09-01 | g91 | 780 |
| 2026-09-01 | e20 | 620 |
| 2026-09-01 | diesel | 1900 |

ค่า `product` ที่รับได้คือ `g95`, `g91`, `e20`, `diesel` และควรมีข้อมูลรายวันต่อเนื่อง เพื่อให้ dropdown 7/14/30 วันคำนวณค่าเฉลี่ยได้ถูกต้อง

## 2. เผยแพร่ Sheet เป็น CSV

1. ใน Google Sheets เลือก **File → Share → Publish to web**
2. เลือกแท็บ `Sales` และรูปแบบ **Comma-separated values (.csv)**
3. กด **Publish** และคัดลอกลิงก์ที่ได้

> ข้อควรทราบ: วิธีนี้ทำให้ข้อมูลในแท็บ `Sales` เป็นสาธารณะสำหรับผู้ที่มีลิงก์ จึงควรใส่เฉพาะยอดขายที่ไม่เป็นข้อมูลลับ และไม่ใส่ชื่อบุคคลหรือข้อมูลลูกค้า

## 3. วางลิงก์ในเว็บไซต์

เปิดไฟล์ `app.js` แล้วแทนที่บรรทัดนี้ด้วยลิงก์ CSV ที่คัดลอกมา:

```js
const SHEET_CSV_URL = "วางลิงก์ CSV ที่นี่";
```

บันทึกไฟล์และเปิด `index.html` อีกครั้ง หน้าจอจะแสดงคำว่า “เชื่อม Google Sheets แล้ว” ด้านขวาบนเมื่อโหลดสำเร็จ

## 4. เปิดเว็บไซต์

ดับเบิลคลิก `index.html` เพื่อดูบนเครื่อง หรืออัปโหลดโฟลเดอร์นี้ไปยังบริการโฮสต์แบบฟรี เช่น **GitHub Pages** หรือ **Cloudflare Pages** เมื่อพร้อมให้ทีมใช้งานร่วมกัน

## การคำนวณ

- เลือกฐานยอดขายแยกในแต่ละสินค้าได้: เฉลี่ย 7 วัน, เฉลี่ย 1 เดือน หรือเฉลี่ย YTD
- ยอดขายเฉลี่ย/วัน = ผลรวมยอดขายของสินค้าในช่วงที่เลือก ÷ จำนวนวันที่มีข้อมูลในช่วงนั้น
- Dead stock = 12% ของความจุถังสินค้านั้น
- StD ปัจจุบัน = (Remaining − Dead stock) ÷ ยอดขายเฉลี่ย/วันแบบที่เลือก
- StD ใหม่ = ((Remaining + ปริมาณ Load ของสินค้านั้น) − Dead stock) ÷ ยอดขายเฉลี่ย/วันแบบที่เลือก
- รถค่าเริ่มต้นคือ 20,000 L มี 5 ช่อง โดยแต่ละช่องเลือก 0 / 3,000 / 4,000 L ได้ ระบบจะแจ้งเมื่อเลือกเกินความจุรถ
